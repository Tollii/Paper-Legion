package tests;

import Database.Database;
import org.junit.*;
import static org.junit.Assert.assertEquals;

public class LoginTest {

    //TODO There are tests classes in hashAndSalt/testing, but they need to be rewritten to work against Database and not the login() and signup() classes.

}